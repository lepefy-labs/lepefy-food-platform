import Link from 'next/link'; import { notFound } from 'next/navigation';
import { IconArrowLeft, IconCalendarEvent, IconGift, IconMail, IconMapPin, IconReceipt, IconTimeline } from '@tabler/icons-react';
import { getTenant } from '@/lib/tenant/getTenant'; import { getCustomerDetail, RFM_LABELS } from '@/lib/admin/crm'; import { createServiceClient } from '@/lib/supabase/server'; import { CustomerActions } from './CustomerActions';
export const dynamic='force-dynamic'; export const fetchCache='force-no-store';
function money(v:number,c:string){return new Intl.NumberFormat('fr-FR',{style:'currency',currency:c}).format(v)} function date(v:string|null){return v?new Intl.DateTimeFormat('fr-FR',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v)):'Jamais'}
function Card({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-2xl border border-[var(--admin-border)] bg-white p-4 shadow-sm dark:bg-gray-900 sm:p-5"><h2 className="mb-4 font-semibold">{title}</h2>{children}</section>}
export default async function CustomerPage({params}:{params:{id:string}}){const tenant=await getTenant(process.env.NEXT_PUBLIC_TENANT_SLUG??'chloefood');const detail=await getCustomerDetail(tenant.id,params.id);if(!detail)notFound();const p=detail.profile;const db=createServiceClient();const [favoriteProduct,favoriteCategory]=await Promise.all([p.favorite_product_id?db.from('products').select('id,name').eq('tenant_id',tenant.id).eq('id',p.favorite_product_id).maybeSingle():Promise.resolve({data:null}),p.favorite_category_id?db.from('categories').select('id,name').eq('tenant_id',tenant.id).eq('id',p.favorite_category_id).maybeSingle():Promise.resolve({data:null})]);
 const attendance=detail.reservations.flatMap((reservation:any)=>reservation.event_reservation_items.flatMap((item:any)=>item.event_reservation_item_redemptions.filter((entry:any)=>!entry.voided_at).map((entry:any)=>({at:entry.redeemed_at,label:'Participation événement',detail:`${reservation.events?.title??'Événement'} · ${entry.quantity_redeemed} entrée${entry.quantity_redeemed>1?'s':''}`,href:`/admin/evenementiel/evenements/${reservation.event_id}`,icon:<IconCalendarEvent size={16}/>}))));
 const timeline=[...detail.orders.map((x:any)=>({at:x.created_at,label:'Commande',detail:`${money(Number(x.total),tenant.currency)} · ${x.status}`,href:`/admin/orders/${x.id}`,icon:<IconReceipt size={16}/>})),...detail.purchases.map((x:any)=>({at:x.created_at,label:'Achat en boutique',detail:`${money(Number(x.amount),tenant.currency)} · ${x.points_awarded} pts`,icon:<IconGift size={16}/>})),...detail.points.map((x:any)=>({at:x.created_at,label:x.amount>=0?'Points gagnés':'Points utilisés',detail:`${x.amount>0?'+':''}${x.amount} pts · ${x.transaction_type}`,icon:<IconGift size={16}/>})),...detail.reservations.map((x:any)=>({at:x.created_at,label:'Réservation événement',detail:`${x.events?.title??'Événement'} · ${money(Number(x.amount_paid),tenant.currency)}`,href:`/admin/evenementiel/evenements/${x.event_id}`,icon:<IconCalendarEvent size={16}/>})),...attendance,...detail.consents.filter((x:any)=>x.consent_type==='marketing').map((x:any)=>({at:x.created_at,label:x.granted?'Consentement marketing accordé':'Consentement marketing refusé',detail:`Source : ${x.source}`,icon:<IconMail size={16}/>})),...detail.events.filter((x:any)=>['customer_created','account_linked'].includes(x.event_type)).map((x:any)=>({at:x.occurred_at,label:x.event_type==='customer_created'?'Client créé':'Compte connecté',detail:`Source : ${x.source}`,icon:<IconTimeline size={16}/>}))].sort((a,b)=>new Date(b.at).getTime()-new Date(a.at).getTime());
 return <div className="mx-auto max-w-7xl space-y-5"><Link href="/admin/clients" className="inline-flex min-h-11 items-center gap-2 text-sm font-semibold text-gray-600"><IconArrowLeft size={18}/>Clients</Link><header className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-2xl font-semibold">{p.full_name??p.email??p.phone??'Client'}</h1><span className="rounded-full bg-violet-50 px-2.5 py-1 text-xs font-semibold text-violet-700">{RFM_LABELS[p.rfm_segment]}</span></div><p className="mt-1 text-sm text-gray-500">{p.email??'E-mail absent'}{p.phone?` · ${p.phone}`:''}</p><p className="mt-1 text-xs text-gray-400">Client depuis {date(p.created_at)}</p></div><CustomerActions customerId={p.id} profile={p}/></header><div className="grid grid-cols-2 gap-3 lg:grid-cols-4">{[['Dépensé',money(p.lifetime_value,tenant.currency)],['Commandes',p.completed_orders_count.toLocaleString('fr-FR')],['Panier moyen',money(p.average_order_value,tenant.currency)],['Dernier achat',date(p.last_order_at)]].map(([l,v])=><article key={l} className="rounded-2xl border border-[var(--admin-border)] bg-white p-4 dark:bg-gray-900"><p className="text-xs text-gray-500">{l}</p><p className="mt-1 text-xl font-semibold">{v}</p></article>)}</div><div className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><Card title="Activité"><div className="space-y-1">{timeline.length===0?<p className="py-8 text-center text-sm text-gray-400">Aucune activité enregistrée.</p>:timeline.slice(0,100).map((item:any,i)=><div key={`${item.at}-${i}`} className="flex gap-3 border-l-2 border-violet-100 py-2 pl-4"><span className="mt-1 text-violet-600">{item.icon}</span><div className="min-w-0 flex-1">{item.href?<Link href={item.href} className="font-medium hover:underline">{item.label}</Link>:<p className="font-medium">{item.label}</p>}<p className="text-xs text-gray-500">{item.detail}</p><p className="mt-1 text-[11px] text-gray-400">{date(item.at)}</p></div></div>)}</div></Card><div className="space-y-5"><Card title="Fidélité"><p className="text-2xl font-semibold">{p.loyalty_points_balance.toLocaleString('fr-FR')} pts</p><p className="mt-1 text-sm text-gray-500">Carte {p.loyalty_card_number??'non attribuée'} · Achats magasin {money(p.in_store_spend,tenant.currency)}</p></Card><Card title="Marketing"><p className={`font-semibold ${p.marketing_consent?'text-emerald-700':'text-gray-500'}`}>{p.marketing_consent?'✓ E-mail autorisé':'Marketing refusé ou non renseigné'}</p><p className="mt-2 text-xs text-gray-500">{p.marketing_consent_at?`Décision du ${date(p.marketing_consent_at)} · Source : ${p.marketing_consent_source}`:'Aucun consentement marketing positif courant.'}</p><p className="mt-2 text-xs text-gray-400">WhatsApp · Non configuré</p></Card><Card title="Produits préférés">{favoriteProduct.data?<Link href={`/admin/catalogue/${favoriteProduct.data.id}`} className="text-sm font-semibold hover:underline">{favoriteProduct.data.name}</Link>:<p className="text-sm">Pas encore assez d’achats</p>}<p className="mt-1 text-xs text-gray-500">Catégorie : {favoriteCategory.data?.name??'—'} · {p.products_count??0} produits achetés</p></Card></div></div><div className="grid gap-5 lg:grid-cols-2"><Card title="Commandes">{detail.orders.length===0?<p className="text-sm text-gray-400">Aucune commande.</p>:<div className="divide-y">{detail.orders.map((o:any)=><Link key={o.id} href={`/admin/orders/${o.id}`} className="flex min-h-11 items-center justify-between py-2 text-sm"><span>#{o.id.slice(0,8).toUpperCase()} · {date(o.created_at)}</span><strong>{money(Number(o.total),tenant.currency)}</strong></Link>)}</div>}</Card><Card title="Coordonnées & adresses"><p className="text-sm">{p.email??'E-mail absent'}<br/>{p.phone??'Téléphone absent'}</p><div className="mt-4 space-y-3">{detail.addresses.map((a:any)=><div key={a.id} className="flex gap-2 rounded-xl bg-gray-50 p-3 text-sm dark:bg-gray-800"><IconMapPin size={17}/><span>{a.line1}{a.line2?`, ${a.line2}`:''}<br/>{a.postal_code} {a.city} · {a.country}</span></div>)}</div></Card><Card title="Événements">{detail.reservations.length===0?<p className="text-sm text-gray-400">Aucune participation reliée.</p>:detail.reservations.map((r:any)=><p key={r.id} className="py-2 text-sm">{r.events?.title??'Événement'} · {date(r.created_at)}</p>)}</Card><Card title="Notes">{detail.notes.length===0?<p className="text-sm text-gray-400">Aucune note.</p>:<div className="space-y-3">{detail.notes.map((n:any)=><article key={n.id} className="rounded-xl bg-amber-50 p-3 dark:bg-amber-950/20"><p className="whitespace-pre-wrap text-sm">{n.body}</p><p className="mt-2 text-[11px] text-gray-500">{n.admin_users?.first_name??n.admin_users?.email??'Admin'} · {date(n.created_at)}</p></article>)}</div>}<div className="mt-4 flex flex-wrap gap-2">{detail.tags.map((t:any)=><span key={t.tag_id} className="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-semibold">{t.customer_tags?.name}</span>)}</div></Card></div><Card title="Campagnes reçues">{detail.campaigns.length===0?<p className="text-sm text-gray-400">Aucune campagne.</p>:detail.campaigns.map((c:any)=><Link key={c.id} href={`/admin/clients/campagnes/${c.marketing_campaigns?.id}`} className="flex min-h-11 items-center justify-between border-b py-2 text-sm"><span>{c.marketing_campaigns?.name??'Campagne'} · {c.marketing_campaigns?.channel}</span><span>{c.status}</span></Link>)}</Card></div>}
